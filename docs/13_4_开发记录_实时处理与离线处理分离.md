# 13_4 开发记录：实时处理与离线处理分离

## 一、背景

### 当前问题

所有图像处理算法（白平衡、CLAHE、去雾、锐化、降噪、边缘检测、阈值分割）都在实时视频流中执行，导致：

1. 开多个算法后 FPS 严重下降（CLAHE + 锐化 + 降噪 → 个位数 FPS）
2. 实时流中跑重算法没有必要——医生实时观察只需要基础增强
3. 锐化、降噪等精细处理应该在拍照后对静态图做，效果更好、不受帧率限制

### 实际内窥镜的工作流

```
实时观察（边看边做）          拍照后处理（存下来再做）
├── 硬件白平衡/曝光（OV5640） ├── 锐化（精细，不限时间）
├── CLAHE 增强（看清暗区）    ├── 降噪（高质量双边滤波）
├── 去雾（水雾环境）          ├── 边缘检测（辅助诊断）
└── 显示 + 拍照键             ├── 阈值分割（辅助分析）
         ↓                    ├── 测量、标注（后续规划）
    保存原图（全分辨率）       └── 导出报告（后续规划）
```

---

## 二、目标架构

### 2.1 三个主要界面

```
┌─────────────────────────────────────────────────────┐
│  [实时采集]    [图库浏览]    [图像编辑]               │
├─────────────────────────────────────────────────────┤
│                                                     │
│              当前选中的界面内容                       │
│                                                     │
├─────────────────────────────────────────────────────┤
│  [拍照]  [录像]  [冻结]  [旋转]     FPS: 20.0      │
└─────────────────────────────────────────────────────┘
```

**实时采集页**（当前主界面，精简后）：
- 视频画面
- 硬件参数：自动白平衡、自动曝光、翻转
- 实时算法：CLAHE、去雾（只保留轻量算法）
- 操作：拍照、录像、冻结、旋转

**图库浏览页**（新增）：
- 缩略图列表，显示 captures/ 下的所有图片
- 点击缩略图 → 全屏查看
- 长按或按钮 → 进入编辑模式
- 删除功能

**图像编辑页**（新增）：
- 显示选中的静态图片（全分辨率）
- 离线算法：锐化、降噪、边缘检测、阈值分割
- 参数滑块实时预览（对静态图处理，不受帧率限制）
- 保存/另存为

### 2.2 数据流

```
实时采集页：
  OV5640 → 硬件白平衡/曝光 → CLAHE/去雾 → 显示
                                          ↓ 拍照
                                    保存原图到 captures/
                                          ↓
图库浏览页：
  读取 captures/ 目录 → 缩略图列表 → 点击查看
                                          ↓ 编辑
图像编辑页：
  加载原图 → 锐化/降噪/边缘检测/阈值分割 → 预览 → 保存
```

### 2.3 拍照保存策略

拍照时保存**原始全分辨率图像**（640x480，未经软件处理），而不是显示尺寸的处理后图像：

```cpp
// 当前（保存显示图像，分辨率低，已处理）
m_displayImage.save(filename, "JPEG", 95);

// 改为（保存原始采集帧，全分辨率，未经软件处理）
m_latestRawFrame.save(filename, "JPEG", 95);
```

这样离线编辑时有最大的处理空间。

---

## 三、实施计划

### Phase 1：存储路径 + 实时界面精简（1 天）

- [ ] 存储路径改到 NFS 共享目录或固定路径
- [ ] 实时采集页只保留：硬件参数 + CLAHE + 去雾
- [ ] 锐化、降噪、边缘检测、阈值分割从实时界面移除
- [ ] 拍照保存原始全分辨率帧

### Phase 2：图库浏览 + 图像编辑模式（2-3 天）

- [ ] 三模式切换：调试模式 / 临床模式 / 图像编辑
- [ ] 图像编辑模式下的右侧参数面板（锐化/降噪/边缘检测/阈值分割）
- [ ] 图像选择弹窗（缩略图列表，触屏滑动翻页）
- [ ] 选中图像显示在视频区域
- [ ] 保存 / 删除功能
- [ ] 进入图像编辑时暂停采集，返回时恢复

### Phase 3：后续扩展（规划中）

- [ ] 测量功能（在图上画线测距离）
- [ ] 标注功能（在图上标记病灶区域）
- [ ] 导出检查报告

---

## 四、与现有优化计划的关系

| 现有计划 | 关系 |
|---|---|
| P3 线程架构优化 | 实时流精简后压力更小，P3 优先级降低 |
| P5-P6 NEON 加速 | 仍然有价值，加速实时流的 CLAHE/去雾 |
| P14 自适应锐化 | 改为在图像编辑页实现，不在实时流中 |
| P1 USB 存储 | 图库浏览页需要明确的存储路径，关联性强 |

---

## 五、当前状态

- [x] 方案设计
- [x] Phase 1：存储路径 + 实时界面精简
- [x] Phase 2：图库浏览 + 图像编辑模式
- [ ] Phase 3：后续扩展

---

## 七、Phase 2 设计方案

### 7.1 三模式切换

顶部三个固定按钮，当前模式置灰：

```
[调试模式] [临床模式] [图像编辑]    ← 三个按钮始终存在，当前模式置灰
```

优点：
- 用户一眼知道有哪些模式
- 不需要记忆按钮文字会变
- 实现简单，不需要动态改文字

### 7.2 三个模式的界面内容

**调试模式：**
```
┌──────────────┬──────────────┐
│              │ 硬件参数       │
│  实时视频     │ 内窥镜算法     │
│              │ [拍照] [录像]  │
│              │ [冻结] [旋转]  │
└──────────────┴──────────────┘
```

**临床模式：**
```
┌──────────────┬──────────────┐
│              │ 选择检查模式   │
│  实时视频     │ [胃镜模式]     │
│              │ [拍照] [录像]  │
│              │ [冻结] [旋转]  │
└──────────────┴──────────────┘
```

**图像编辑模式：**
```
┌──────────────┬──────────────┐
│              │ [图像选择]      │
│  静态图像     │ 锐化  [滑块]   │
│ （或黑屏）    │ 降噪  边缘检测 │
│              │ 阈值分割 [滑块] │
│              │ [保存] [删除]  │
└──────────────┴──────────────┘
```

### 7.3 图像选择弹窗

点击“图像选择”按钮后，弹出 QDialog 全屏显示缩略图列表：

```
┌─────────────────────────────┐
│  选择图像                        │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐  │
│  │   │ │   │ │   │ │   │  │
│  └───┘ └───┘ └───┘ └───┘  │  ← 一行 4 张
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐  │
│  │   │ │   │ │   │ │   │  │
│  └───┘ └───┘ └───┘ └───┘  │  ← 触屏上下滑动翻页
│                             │
│              [取消]          │
└─────────────────────────────┘
```

- 扫描 `m_savePath + "/captures/"` 目录
- 生成缩略图（120x90）
- 点击缩略图 → 关闭弹窗，加载原图到视频显示区域
- 触屏滑动翻页（QScrollArea + QScroller）

### 7.4 采集暂停/恢复

- 进入图像编辑模式：暂停采集线程，FPS 显示“--”
- 返回调试/临床模式：恢复采集线程

### 7.5 代码改动范围

| 文件 | 改动内容 |
|---|---|
| `src/ui/mainwindow.h` | 新增图像编辑模式相关成员变量、槽函数 |
| `src/ui/mainwindow.cpp` | 三模式切换逻辑、图像编辑面板、图像选择弹窗、采集暂停/恢复 |
| `src/processing/imageprocessor.h/cpp` | 无需修改，复用现有算法 |

---

## 六、Phase 1 代码修改详细记录

### 6.1 mainwindow.h — 新增原始帧缓存

**新增：**

```cpp
    // --- 拍照用原始帧缓存 ---
    QImage m_latestRawFrame;       // 最新原始帧（全分辨率，未经软件处理）
```

### 6.2 mainwindow.cpp — 存储路径改为 NFS 共享目录

**改动前：**

```cpp
    QDir().mkpath("./captures");
    QDir().mkpath("./videos");
```

**改动后：**

```cpp
    QDir().mkpath("/mnt/nfs/captures");
    QDir().mkpath("/mnt/nfs/videos");
```

### 6.3 mainwindow.cpp — setupUi()：移除通用算法分组框

通用算法（锐化/降噪/边缘检测/阈值分割）从实时界面移除，待 Phase 3 在图像编辑页实现。控件保留但不显示，避免其他代码引用报错。

**改动前：**

```cpp
    // -- 通用算法分组框 --
    QGroupBox *generalGroup = new QGroupBox("通用算法");
    QVBoxLayout *generalLayout = new QVBoxLayout(generalGroup);
    generalLayout->setSpacing(2);

    m_chkSharpen = new QCheckBox("锐化");
    m_sliderSharpen = new QSlider(Qt::Horizontal);
    m_sliderSharpen->setRange(5, 30);
    m_sliderSharpen->setValue(15);

    m_chkDenoise = new QCheckBox("降噪");
    m_chkEdgeDetect = new QCheckBox("边缘检测");
    m_chkThreshold = new QCheckBox("阈值分割");
    m_sliderThreshold = new QSlider(Qt::Horizontal);
    m_sliderThreshold->setRange(0, 255);
    m_sliderThreshold->setValue(128);

    generalLayout->addWidget(m_chkSharpen);
    generalLayout->addWidget(m_sliderSharpen);
    generalLayout->addWidget(m_chkDenoise);
    generalLayout->addWidget(m_chkEdgeDetect);
    generalLayout->addWidget(m_chkThreshold);
    generalLayout->addWidget(m_sliderThreshold);
    makeCollapsible(generalGroup, false);
    debugLayout->addWidget(generalGroup);
```

**改动后：**

```cpp
    // -- 通用算法分组框（已移至图像编辑页，保留控件但不显示）--
    m_chkSharpen = new QCheckBox;
    m_sliderSharpen = new QSlider;
    m_chkDenoise = new QCheckBox;
    m_chkEdgeDetect = new QCheckBox;
    m_chkThreshold = new QCheckBox;
    m_sliderThreshold = new QSlider;
```

### 6.4 mainwindow.cpp — onFrameReady()：保存原始帧

**改动前：**

```cpp
void MainWindow::onFrameReady(const QImage &image) {
    if (m_frozen) {
        m_source->m_pendingFrames.fetch_sub(1);
        return;
    }
    m_frameCount++;
```

**改动后：**

```cpp
void MainWindow::onFrameReady(const QImage &image) {
    if (m_frozen) {
        m_source->m_pendingFrames.fetch_sub(1);
        return;
    }
    m_frameCount++;

    // 保存原始帧（全分辨率，用于拍照）
    m_latestRawFrame = image;
```

### 6.5 mainwindow.cpp — processFrame()：通用算法固定关闭

**改动前：**

```cpp
        m_procConfig.sharpen = m_chkSharpen->isChecked();
        m_procConfig.sharpenAmount = m_sliderSharpen->value() / 10.0;
        m_procConfig.denoise = m_chkDenoise->isChecked();
        m_procConfig.edgeDetect = m_chkEdgeDetect->isChecked();
        m_procConfig.threshold = m_chkThreshold->isChecked();
        m_procConfig.thresholdValue = m_sliderThreshold->value();
```

**改动后：**

```cpp
        // 通用算法已移至图像编辑页，实时流中不启用
        m_procConfig.sharpen = false;
        m_procConfig.denoise = false;
        m_procConfig.edgeDetect = false;
        m_procConfig.threshold = false;
```

### 6.6 mainwindow.cpp — onCapturePhoto()：保存原始帧到 NFS

**改动前：**

```cpp
void MainWindow::onCapturePhoto() {
    if (m_displayImage.isNull()) return;
    QString filename = QString("./captures/%1.jpg")
        .arg(QDateTime::currentDateTime().toString("yyyyMMdd_hhmmss"));
    m_displayImage.save(filename, "JPEG", 95);
    m_lblStatus->setText("拍照: " + filename);
}
```

**改动后：**

```cpp
void MainWindow::onCapturePhoto() {
    if (m_displayImage.isNull() && m_latestRawFrame.isNull()) return;
    QString filename = QString("/mnt/nfs/captures/%1.jpg")
        .arg(QDateTime::currentDateTime().toString("yyyyMMdd_hhmmss"));
    // 保存原始全分辨率帧，为离线编辑保留最大处理空间
    if (!m_latestRawFrame.isNull())
        m_latestRawFrame.save(filename, "JPEG", 95);
    else
        m_displayImage.save(filename, "JPEG", 95);
    m_lblStatus->setText("拍照: " + filename);
}
```

### 6.7 mainwindow.cpp — onToggleRecord()：录像路径改为 NFS

**改动前：**

```cpp
        QString filename = QString("./videos/%1.avi")
```

**改动后：**

```cpp
        QString filename = QString("/mnt/nfs/videos/%1.avi")
```

### 6.8 mainwindow.cpp — onExportPreset()：通用算法固定关闭

**改动前：**

```cpp
    cfg.sharpen        = m_chkSharpen->isChecked();
    cfg.sharpenAmount  = m_sliderSharpen->value() / 10.0;
    cfg.denoise        = m_chkDenoise->isChecked();
    cfg.edgeDetect     = m_chkEdgeDetect->isChecked();
    cfg.threshold      = m_chkThreshold->isChecked();
    cfg.thresholdValue = m_sliderThreshold->value();
```

**改动后：**

```cpp
    cfg.sharpen        = false;
    cfg.denoise        = false;
    cfg.edgeDetect     = false;
    cfg.threshold      = false;
```

### 6.9 mainwindow.cpp — applyConfig()：移除通用算法同步

**改动前：**

```cpp
void MainWindow::applyConfig(const ImageProcessor::Config &cfg) {
    m_chkClahe->setChecked(cfg.clahe);
    m_sliderClaheClip->setValue(static_cast<int>(cfg.claheClipLimit * 10));
    m_chkUndistort->setChecked(cfg.undistort && m_undistortReady);
    m_chkDehaze->setChecked(cfg.dehaze);
    m_chkSharpen->setChecked(cfg.sharpen);
    m_sliderSharpen->setValue(static_cast<int>(cfg.sharpenAmount * 10));
    m_chkDenoise->setChecked(cfg.denoise);
    m_chkEdgeDetect->setChecked(cfg.edgeDetect);
    m_chkThreshold->setChecked(cfg.threshold);
    m_sliderThreshold->setValue(cfg.thresholdValue);
}
```

**改动后：**

```cpp
void MainWindow::applyConfig(const ImageProcessor::Config &cfg) {
    m_chkClahe->setChecked(cfg.clahe);
    m_sliderClaheClip->setValue(static_cast<int>(cfg.claheClipLimit * 10));
    m_chkUndistort->setChecked(cfg.undistort && m_undistortReady);
    m_chkDehaze->setChecked(cfg.dehaze);
}
```

### 6.10 未修改的文件

| 文件 | 说明 |
|---|---|
| `imageprocessor.h/cpp` | 算法代码保留，Phase 3 图像编辑页会复用 |
| `v4l2capture.h/cpp` | 无关 |
| `videosource.h` | 无关 |


---

## 八、Phase 2 代码修改详细记录

### 8.1 mainwindow.h — 新增成员变量和槽函数

**新增槽函数：**

```cpp
    void onEnterEditMode();                    // 进入图像编辑模式
    void onSelectImage();                      // 打开图像选择弹窗
    void onSaveEditedImage();                  // 保存编辑后的图像
    void onDeleteImage();                      // 删除当前图像
    void onEditParamChanged();                 // 编辑参数变化时实时预览
```

**新增私有方法：**

```cpp
    void switchToMode(int mode);  // 0=调试, 1=临床, 2=图像编辑
```

**新增成员变量：**

```cpp
    // --- 当前模式：0=调试, 1=临床, 2=图像编辑 ---
    int m_currentMode;

    // --- 模式切换按钮（三个固定按钮）---
    QPushButton *m_btnModeDebug;
    QPushButton *m_btnModeClinical;
    QPushButton *m_btnModeEdit;

    // --- 图像编辑面板和控件 ---
    QWidget *m_editPanel;
    QPushButton *m_btnSelectImage;
    QPushButton *m_btnSaveEdit;
    QPushButton *m_btnDeleteImage;
    QCheckBox *m_chkEditSharpen;
    QSlider *m_sliderEditSharpen;
    QCheckBox *m_chkEditDenoise;
    QCheckBox *m_chkEditEdge;
    QCheckBox *m_chkEditThreshold;
    QSlider *m_sliderEditThreshold;
    QImage m_editSourceImage;         // 编辑源图（原始全分辨率）
    QString m_editImagePath;          // 当前编辑图像的文件路径
```

**删除的成员变量：**

```cpp
    QPushButton *m_btnSwitchMode;     // 被三个模式按钮替代
```

### 8.2 mainwindow.cpp — 构造函数初始化列表

**改动前：**

```cpp
      m_clinicalMode(false)
{
```

**改动后：**

```cpp
      m_clinicalMode(false),
      m_currentMode(0)
{
```

### 8.3 mainwindow.cpp — setupUi()：模式切换按钮

**改动前：**

```cpp
    // --- 模式切换按钮（最顶部）---
    m_btnSwitchMode = new QPushButton("切换到临床模式");
    connect(m_btnSwitchMode, &QPushButton::clicked, this, &MainWindow::onSwitchMode);
    rightLayout->addWidget(m_btnSwitchMode);

    // --- QStackedWidget: 面板切换容器 ---
    // index 0 = 调试面板，index 1 = 临床面板
    m_ctrlStack = new QStackedWidget;
```

**改动后：**

```cpp
    // --- 模式切换按钮（三个固定按钮，当前模式置灰）---
    QHBoxLayout *modeLayout = new QHBoxLayout;
    modeLayout->setSpacing(2);
    m_btnModeDebug = new QPushButton("调试");
    m_btnModeClinical = new QPushButton("临床");
    m_btnModeEdit = new QPushButton("编辑");
    QString modeStyle = "QPushButton { font-size: 12px; }";
    m_btnModeDebug->setStyleSheet(modeStyle);
    m_btnModeClinical->setStyleSheet(modeStyle);
    m_btnModeEdit->setStyleSheet(modeStyle);
    modeLayout->addWidget(m_btnModeDebug);
    modeLayout->addWidget(m_btnModeClinical);
    modeLayout->addWidget(m_btnModeEdit);
    rightLayout->addLayout(modeLayout);

    connect(m_btnModeDebug, &QPushButton::clicked, [this]() { switchToMode(0); });
    connect(m_btnModeClinical, &QPushButton::clicked, [this]() { switchToMode(1); });
    connect(m_btnModeEdit, &QPushButton::clicked, [this]() { switchToMode(2); });

    // --- QStackedWidget: 面板切换容器 ---
    // index 0 = 调试, index 1 = 临床, index 2 = 图像编辑
    m_ctrlStack = new QStackedWidget;
```

### 8.4 mainwindow.cpp — setupUi()：新增图像编辑面板

在临床面板之后、ctrlStack 加入右侧布局之前，新增图像编辑面板（index 2）：

```cpp
    // ==============================================================
    // 图像编辑面板（index 2）
    // ==============================================================
    m_editPanel = new QWidget;
    QVBoxLayout *editLayout = new QVBoxLayout(m_editPanel);
    editLayout->setSpacing(4);
    editLayout->setContentsMargins(0, 0, 0, 0);

    m_btnSelectImage = new QPushButton("图像选择");
    connect(m_btnSelectImage, &QPushButton::clicked, this, &MainWindow::onSelectImage);
    editLayout->addWidget(m_btnSelectImage);

    // 编辑算法控件
    QGroupBox *editGroup = new QGroupBox("图像处理");
    QVBoxLayout *editAlgoLayout = new QVBoxLayout(editGroup);
    editAlgoLayout->setSpacing(2);

    m_chkEditSharpen = new QCheckBox("锐化");
    m_sliderEditSharpen = new QSlider(Qt::Horizontal);
    m_sliderEditSharpen->setRange(5, 30);
    m_sliderEditSharpen->setValue(15);
    m_chkEditDenoise = new QCheckBox("降噪");
    m_chkEditEdge = new QCheckBox("边缘检测");
    m_chkEditThreshold = new QCheckBox("阈值分割");
    m_sliderEditThreshold = new QSlider(Qt::Horizontal);
    m_sliderEditThreshold->setRange(0, 255);
    m_sliderEditThreshold->setValue(128);

    editAlgoLayout->addWidget(m_chkEditSharpen);
    editAlgoLayout->addWidget(m_sliderEditSharpen);
    editAlgoLayout->addWidget(m_chkEditDenoise);
    editAlgoLayout->addWidget(m_chkEditEdge);
    editAlgoLayout->addWidget(m_chkEditThreshold);
    editAlgoLayout->addWidget(m_sliderEditThreshold);
    editLayout->addWidget(editGroup);

    // 参数变化时实时预览
    connect(m_chkEditSharpen, &QCheckBox::toggled, this, &MainWindow::onEditParamChanged);
    connect(m_sliderEditSharpen, &QSlider::valueChanged, this, &MainWindow::onEditParamChanged);
    connect(m_chkEditDenoise, &QCheckBox::toggled, this, &MainWindow::onEditParamChanged);
    connect(m_chkEditEdge, &QCheckBox::toggled, this, &MainWindow::onEditParamChanged);
    connect(m_chkEditThreshold, &QCheckBox::toggled, this, &MainWindow::onEditParamChanged);
    connect(m_sliderEditThreshold, &QSlider::valueChanged, this, &MainWindow::onEditParamChanged);

    // 保存和删除按钮
    QHBoxLayout *editBtnLayout = new QHBoxLayout;
    m_btnSaveEdit = new QPushButton("保存");
    m_btnDeleteImage = new QPushButton("删除");
    connect(m_btnSaveEdit, &QPushButton::clicked, this, &MainWindow::onSaveEditedImage);
    connect(m_btnDeleteImage, &QPushButton::clicked, this, &MainWindow::onDeleteImage);
    editBtnLayout->addWidget(m_btnSaveEdit);
    editBtnLayout->addWidget(m_btnDeleteImage);
    editLayout->addLayout(editBtnLayout);

    editLayout->addStretch();
    m_ctrlStack->addWidget(m_editPanel);
```

### 8.5 mainwindow.cpp — setupUi()：初始模式设置

**改动前：**

```cpp
    m_ctrlStack->setCurrentIndex(0);
```

**改动后：**

```cpp
    m_currentMode = -1;  // 强制首次切换生效
    switchToMode(0);
```

### 8.6 mainwindow.cpp — switchToMode()：替代 onSwitchMode

**改动前（onSwitchMode）：**

```cpp
void MainWindow::onSwitchMode() {
    m_clinicalMode = !m_clinicalMode;
    if (m_clinicalMode) {
        m_ctrlStack->setCurrentIndex(1);
        m_btnSwitchMode->setText("切换到调试模式");
        if (!m_presetConfigs.isEmpty())
            onPresetSelected(m_cboPreset->currentIndex());
        m_lblStatus->setText("临床模式");
    } else {
        m_ctrlStack->setCurrentIndex(0);
        m_btnSwitchMode->setText("切换到临床模式");
        m_lblStatus->setText("调试模式");
    }
}
```

**改动后（switchToMode）：**

```cpp
void MainWindow::switchToMode(int mode) {
    if (mode == m_currentMode) return;

    // 离开图像编辑模式时恢复采集
    if (m_currentMode == 2 && mode != 2) {
        if (m_source && !m_source->isRunning())
            m_source->start();
    }

    m_currentMode = mode;
    m_ctrlStack->setCurrentIndex(mode);

    // 更新按钮状态：当前模式置灰
    m_btnModeDebug->setEnabled(mode != 0);
    m_btnModeClinical->setEnabled(mode != 1);
    m_btnModeEdit->setEnabled(mode != 2);

    // 图像编辑模式下隐藏拍照等按钮，其他模式显示
    bool showCaptureButtons = (mode != 2);
    m_btnCapture->setVisible(showCaptureButtons);
    m_btnRecord->setVisible(showCaptureButtons);
    m_btnFreeze->setVisible(showCaptureButtons);
    m_btnRotate->setVisible(showCaptureButtons);

    switch (mode) {
    case 0:  // 调试模式
        m_clinicalMode = false;
        m_lblStatus->setText("调试模式");
        break;
    case 1:  // 临床模式
        m_clinicalMode = true;
        if (!m_presetConfigs.isEmpty())
            onPresetSelected(m_cboPreset->currentIndex());
        m_lblStatus->setText("临床模式");
        break;
    case 2:  // 图像编辑模式
        m_clinicalMode = false;
        if (m_source && m_source->isRunning())
            m_source->stop();
        m_lblStatus->setText("图像编辑");
        m_lblFps->setText("FPS: --");
        break;
    }
}

// 兼容旧接口
void MainWindow::onSwitchMode() {
    switchToMode(m_currentMode == 0 ? 1 : 0);
}
```

### 8.7 mainwindow.cpp — onSelectImage()：图像选择弹窗

**新增函数：**

```cpp
void MainWindow::onSelectImage() {
    QString dir = m_savePath + "/captures";
    QDir captureDir(dir);
    QStringList files = captureDir.entryList(QStringList() << "*.jpg" << "*.png",
                                              QDir::Files, QDir::Time);
    if (files.isEmpty()) {
        m_lblStatus->setText("无图像文件");
        return;
    }

    // 创建全屏弹窗
    QDialog dlg(this);
    dlg.setWindowTitle("选择图像");
    dlg.setFixedSize(this->size());

    QVBoxLayout *dlgLayout = new QVBoxLayout(&dlg);

    // 缩略图网格
    QScrollArea *scroll = new QScrollArea;
    QWidget *grid = new QWidget;
    QGridLayout *gridLayout = new QGridLayout(grid);
    gridLayout->setSpacing(8);

    int col = 0, row = 0;
    QString selectedFile;

    for (const QString &file : files) {
        QString path = dir + "/" + file;
        QPixmap thumb(path);
        thumb = thumb.scaled(120, 90, Qt::KeepAspectRatio, Qt::SmoothTransformation);

        QPushButton *btn = new QPushButton;
        btn->setIcon(QIcon(thumb));
        btn->setIconSize(thumb.size());
        btn->setFixedSize(130, 100);

        connect(btn, &QPushButton::clicked, [&dlg, &selectedFile, path]() {
            selectedFile = path;
            dlg.accept();
        });

        gridLayout->addWidget(btn, row, col);
        col++;
        if (col >= 4) { col = 0; row++; }
    }

    scroll->setWidget(grid);
    scroll->setWidgetResizable(true);
    QScroller::grabGesture(scroll->viewport(), QScroller::TouchGesture);
    dlgLayout->addWidget(scroll);

    QPushButton *btnCancel = new QPushButton("取消");
    connect(btnCancel, &QPushButton::clicked, &dlg, &QDialog::reject);
    dlgLayout->addWidget(btnCancel);

    if (dlg.exec() == QDialog::Accepted && !selectedFile.isEmpty()) {
        m_editSourceImage = QImage(selectedFile);
        m_editImagePath = selectedFile;
        m_displayImage = m_editSourceImage.scaled(m_videoRect.size(),
                                                   Qt::KeepAspectRatio, Qt::SmoothTransformation);
        update();
        m_lblStatus->setText("编辑: " + QFileInfo(selectedFile).fileName());
    }
}
```

### 8.8 mainwindow.cpp — onEditParamChanged()：实时预览

**新增函数：**

```cpp
void MainWindow::onEditParamChanged() {
    if (m_editSourceImage.isNull()) return;

    cv::Mat src = ImageConvert::qimageToMat(m_editSourceImage);
    if (src.empty()) return;

    ImageProcessor::Config cfg;
    cfg.sharpen = m_chkEditSharpen->isChecked();
    cfg.sharpenAmount = m_sliderEditSharpen->value() / 10.0;
    cfg.denoise = m_chkEditDenoise->isChecked();
    cfg.edgeDetect = m_chkEditEdge->isChecked();
    cfg.threshold = m_chkEditThreshold->isChecked();
    cfg.thresholdValue = m_sliderEditThreshold->value();

    cv::Mat dst;
    ImageProcessor::process(src, dst, cfg);

    cv::Mat display;
    cv::resize(dst, display, cv::Size(m_videoRect.width(), m_videoRect.height()));
    m_displayImage = ImageConvert::matToQImage(display);
    update();
}
```

### 8.9 mainwindow.cpp — onSaveEditedImage() / onDeleteImage()

**新增函数：**

```cpp
void MainWindow::onSaveEditedImage() {
    if (m_editSourceImage.isNull()) {
        m_lblStatus->setText("无图像可保存");
        return;
    }

    cv::Mat src = ImageConvert::qimageToMat(m_editSourceImage);
    ImageProcessor::Config cfg;
    cfg.sharpen = m_chkEditSharpen->isChecked();
    cfg.sharpenAmount = m_sliderEditSharpen->value() / 10.0;
    cfg.denoise = m_chkEditDenoise->isChecked();
    cfg.edgeDetect = m_chkEditEdge->isChecked();
    cfg.threshold = m_chkEditThreshold->isChecked();
    cfg.thresholdValue = m_sliderEditThreshold->value();

    cv::Mat dst;
    ImageProcessor::process(src, dst, cfg);

    QFileInfo fi(m_editImagePath);
    QString savePath = fi.path() + "/" + fi.baseName() + "_edited.jpg";
    QImage result = ImageConvert::matToQImage(dst);
    result.save(savePath, "JPEG", 95);
    m_lblStatus->setText("已保存: " + QFileInfo(savePath).fileName());
}

void MainWindow::onDeleteImage() {
    if (m_editImagePath.isEmpty()) {
        m_lblStatus->setText("无图像可删除");
        return;
    }
    QFile::remove(m_editImagePath);
    m_editSourceImage = QImage();
    m_editImagePath.clear();
    m_displayImage = QImage();
    update();
    m_lblStatus->setText("图像已删除");
}
```

### 8.10 mainwindow.cpp — 新增 include

```cpp
#include <QDialog>
#include <QFile>
```

### 8.11 未修改的文件

| 文件 | 说明 |
|---|---|
| `imageprocessor.h/cpp` | 无需修改，编辑页复用现有算法 |
| `v4l2capture.h/cpp` | 无需修改 |
| `videosource.h` | 无需修改，`stop()`/`start()`/`isRunning()` 已有 |

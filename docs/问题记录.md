# 问题记录

开发过程中遇到的问题及解决方案。

---

## 问题 #1：bilateralFilter 崩溃

**现象**
```
error: (-215:Assertion failed) src.data != dst.data in function 'bilateralFilter_8u'
terminate called after throwing an instance of 'cv::Exception'
已中止 (核心已转储)
```

**原因**

`ImageProcessor::process` 中所有算法都以 `dst, dst` 方式调用（输入输出同一个 Mat）。OpenCV 的 `bilateralFilter` 不允许原地操作（src 和 dst 不能指向同一块内存）。

**修复**

`src/processing/imageprocessor.cpp` 中 `applyDenoise` 加临时变量中转：

```cpp
// 修复前
cv::bilateralFilter(src, dst, d, sigmaColor, sigmaSpace);

// 修复后
cv::Mat tmp;
cv::bilateralFilter(src, tmp, d, sigmaColor, sigmaSpace);
dst = tmp;
```

**文件**: `src/processing/imageprocessor.cpp`

---

## 问题 #2：camera_calib.yml 找不到

**现象**
```
[ERROR:0@0.219] global persistence.cpp (505) open Can't open file: 'camera_calib.yml' in read mode
```

**原因**

项目未附带相机标定文件，这是正常的。标定文件需要用实际摄像头拍摄棋盘格图案后生成。

**影响**

不影响运行，畸变校正功能不可用（界面上该选项为灰色），其他功能正常。

---

## 问题 #3：临床模式切换后画面严重卡顿

**来源**：`11_开发记录_调试临床模式切换` 开发过程中

**现象**

切换到临床模式后，预设参数同时开启白平衡+CLAHE+去雾+锐化+降噪，帧率从 30fps 骤降到个位数，画面明显卡顿。

**原因分析**

两个性能瓶颈：

1. **去雾算法（applyDehaze）**：使用 `CV_64FC3`（双精度浮点），每帧需要多次 split/merge/erode，计算量极大
2. **降噪算法（applyDenoise）**：`bilateralFilter` 是 OpenCV 中最慢的滤波器之一，768×576 分辨率下每帧耗时约 30-50ms

**修复**

文件：`src/processing/imageprocessor.cpp`

### 修复 1：去雾算法 — 双精度改单精度 + 减少冗余操作

```cpp
// ===== 修复前 =====
void ImageProcessor::applyDehaze(const cv::Mat &src, cv::Mat &dst,
                                 double omega, int radius) {
    cv::Mat srcFloat;
    src.convertTo(srcFloat, CV_64FC3, 1.0 / 255.0);  // 双精度，慢

    std::vector<cv::Mat> channels;
    cv::split(srcFloat, channels);
    cv::Mat darkChannel = cv::min(cv::min(channels[0], channels[1]), channels[2]);
    // ... 省略中间 ...

    // 透射率计算：多余的 split/merge/split
    std::vector<cv::Mat> normChannels;
    cv::split(srcFloat, normChannels);
    for (int i = 0; i < 3; i++)
        normChannels[i] /= (A[i] + 1e-6);

    cv::Mat normImg;
    cv::merge(normChannels, normImg);     // 多余的 merge
    cv::split(normImg, normChannels);     // 又 split 回来

    cv::Mat transmission = cv::min(cv::min(normChannels[0], normChannels[1]),
                                   normChannels[2]);
    // ...
}

// ===== 修复后 =====
void ImageProcessor::applyDehaze(const cv::Mat &src, cv::Mat &dst,
                                 double omega, int radius) {
    cv::Mat srcFloat;
    src.convertTo(srcFloat, CV_32FC3, 1.0f / 255.0f);  // 单精度，速度翻倍

    std::vector<cv::Mat> channels;
    cv::split(srcFloat, channels);
    cv::Mat darkChannel;
    cv::min(channels[0], channels[1], darkChannel);     // 避免临时对象
    cv::min(darkChannel, channels[2], darkChannel);
    // ... 省略中间 ...

    // 透射率计算：直接 divide，省去多余的 merge/split
    cv::Mat normMin;
    cv::Mat c0, c1, c2;
    cv::divide(channels[0], A[0] + 1e-6, c0);
    cv::divide(channels[1], A[1] + 1e-6, c1);
    cv::divide(channels[2], A[2] + 1e-6, c2);
    cv::min(c0, c1, normMin);
    cv::min(normMin, c2, normMin);
    // ...
}
```

优化点：
- `CV_64FC3` → `CV_32FC3`：单精度浮点运算速度约为双精度的 2 倍
- 去掉透射率计算中多余的 `merge` + `split`，直接用 `cv::divide` 逐通道计算
- `cv::min` 用两参数版本避免创建临时 Mat 对象

### 修复 2：降噪算法 — 缩小后处理再放大

```cpp
// ===== 修复前 =====
void ImageProcessor::applyDenoise(const cv::Mat &src, cv::Mat &dst,
                                  int d, double sigmaColor,
                                  double sigmaSpace) {
    cv::Mat tmp;
    cv::bilateralFilter(src, tmp, d, sigmaColor, sigmaSpace);  // 全分辨率处理，非常慢
    dst = tmp;
}

// ===== 修复后 =====
void ImageProcessor::applyDenoise(const cv::Mat &src, cv::Mat &dst,
                                  int d, double sigmaColor,
                                  double sigmaSpace) {
    cv::Mat small, tmp;
    cv::resize(src, small, cv::Size(), 0.5, 0.5, cv::INTER_LINEAR);  // 缩小到 50%
    cv::bilateralFilter(small, tmp, d, sigmaColor, sigmaSpace);       // 在小图上滤波
    cv::resize(tmp, dst, src.size(), 0, 0, cv::INTER_LINEAR);        // 放大回原尺寸
}
```

优化点：
- 像素量减少 75%（768×576 → 384×288），双边滤波速度提升约 4 倍
- 降噪本身就是模糊操作，缩小再放大的视觉差异很小

**效果**

临床模式下帧率从个位数恢复到可用水平，画面流畅度明显改善。

> → FPS 问题的完整跟踪见 `16_FPS性能问题总跟踪.md`

---

## 问题 #4：代码替换导致函数合并错误

**来源**：修复问题 #3 过程中

**现象**

使用工具替换去雾函数末尾时，`applyDehaze` 的右花括号和 `applyWhiteBalance` 的函数签名被合并成一行：

```cpp
// 错误的合并结果
    dst.convertTo(dst, CV_8UC3, 255.0);
}(const cv::Mat &src, cv::Mat &dst) {   // applyWhiteBalance 的签名粘在了 } 后面
```

**原因**

替换时 `oldStr` 的范围不精确，把两个函数之间的边界吞掉了。

**修复**

重写整个 `imageprocessor.cpp` 文件，确保所有函数边界正确。

**教训**

对文件做多处替换时，需要仔细检查替换边界，避免跨函数的意外合并。修改后应编译验证。

---

## 问题 #5：冻结画面不清晰（运动模糊）

**来源**：日常测试中发现

**现象**

按下冻结后，显示的帧呈现运动模糊，不够清晰。

**原因**

原实现直接停在最后一帧，而视频流中每帧都带有不同程度的运动模糊（曝光时间内画面移动导致）。最后一帧不一定是最清晰的。

**修复**

缓存最近 8 帧，冻结时用拉普拉斯方差评估每帧清晰度，选得分最高的帧显示。

详见：`docs/12_开发记录_冻结帧清晰度优化.md`

**文件**：`src/processing/imageprocessor.h/cpp`、`src/ui/mainwindow.h/cpp`

---

## 问题 #6：交叉编译在 VM_Share 下报错“文件被截断”

**来源**：交叉编译部署阶段

**现象**
```
file not recognized: 文件被截断
collect2: 错误： ld 返回 1
```

**原因**

在 `/mnt/hgfs/VM_Share/` 共享目录下创建 `build_arm` 并编译。VM_Share 是 VMware 的 hgfs 共享文件夹，多线程并行编译时 I/O 不稳定，导致 .o 文件写入不完整。

**修复**

在虚拟机本地目录编译，不在共享目录下编译：

```bash
# 正确做法
mkdir -p /home/chris/build_arm && cd /home/chris/build_arm
cmake /mnt/hgfs/VM_Share/project/EndoscopeSystem ...
make -j$(nproc)
```

**文件**：无代码改动，编译环境问题

---

## 问题 #7：开发板上移动摄像头时局部花屏

**来源**：开发板实际运行测试

**现象**

移动摄像头时，画面局部出现花屏（擕裂），感觉是刷新没跟上。

**初始猜想与排查**

| 猜想 | 排查结果 |
|---|---|
| 画面擕裂（tearing） | ✘ 不是，tearing 是水平分界线，实际是随机亮线条 |
| V4L2 buffer 覆盖 | ✔ **属于这个范畴**，但具体机制是归还时机错误，不是 buffer 数量不足 |
| YUYV 转换错误 | ✘ 静止时正常，排除 |
| 带宽不足 | ✘ 本地运行也花，排除 |
| NFS 运行导致 | ✘ 本地运行也花，排除 |

**最终原因**

V4L2 buffer 读写冲突：`grabFrame()` 中 DQBUF 取出缓冲区后立刻 QBUF 归还，但 `run()` 中的 `cvtColor` 还在读取该 buffer 的数据。驱动归还后可能立刻往 buffer 写入新帧，导致一帧中混合新旧数据，新数据区域亮度不同产生亮线条。

**排查方法：**

**花屏可能的原因有几个：**
可能原因 现象特征 排查方法
画面撕裂（tearing） 水平分界线，上半部分和下半部分不一致 冻结画面后看是否正常
V4L2 buffer 覆盖 随机位置出现色块或乱码 减少帧率看是否消失
YUYV 转换错误 固定位置出现颜色异常 静止不动时是否也有
带宽不足 整帧或大面积花屏 降低分辨率看是否消失
NFS 运行导致 程序在 NFS 上运行，I/O 抢占 CPU 复制到本地运行对比

**你在开发板上做几个测试：**

测试 1：静止时是否花屏

摄像头对着不动的物体，观察画面是否正常
→ 正常：说明是运动时才出现，倾向于 tearing 或 buffer 覆盖
→ 也花：说明是转换错误或带宽问题
测试结果：静止时不花屏

测试 2：冻结后是否正常

按空格冻结画面，看冻结的那一帧是否完整清晰
→ 完整：说明单帧数据没问题，是显示刷新时的撕裂
→ 也花：说明采集到的数据本身就有问题
测试结果：不花

测试 3：花屏的形状

观察花屏的样子：
→ 水平一条线分界，上下内容不一致 → tearing（撕裂）
→ 随机位置出现绿色/粉色色块 → YUYV 数据不完整或 buffer 被覆盖
→ 整帧闪烁 → 带宽或帧率问题
测试结果：有新的物品进入画面时，新物品的区域内随机出现一些宽的线条，线条的亮度比其他区域更亮

测试 4：复制到本地运行

开发板上
cp /mnt/nfs/endoscope /root/
cp -r /mnt/nfs/config /root/
cd /root
./endoscope -platform linuxfb

对比 NFS 运行和本地运行是否有区别
→ 本地不花：NFS I/O 抢占导致
→ 都花：和 NFS 无关
测试结果：都花

**解决方案**

将 QBUF 归还操作从 `grabFrame()` 移到 `run()` 中，确保 `cvtColor` 完成数据拷贝后再归还 buffer：

**修复前** (`grabFrame` 中立刻归还 buffer)：

```cpp
// grabFrame — DQBUF 后立刻 QBUF
bool V4l2Capture::grabFrame(void **data, int *size) {
    // ... select 等待 ...
    if (xioctl(m_fd, VIDIOC_DQBUF, &buf) < 0) return false;
    *data = m_buffers[buf.index].start;
    *size = buf.bytesused;
    if (xioctl(m_fd, VIDIOC_QBUF, &buf) < 0) return false;  // 立刻归还！
    return true;
}

// run — 读取时 buffer 可能已被驱动覆写
void V4l2Capture::run() {
    while (m_running) {
        void *data; int size;
        grabFrame(&data, &size);
        cv::Mat yuyv(m_height, m_width, CV_8UC2, data);  // data 指向已归还的 buffer
        cv::cvtColor(yuyv, bgr, cv::COLOR_YUV2BGR_YUYV); // 读写冲突！
        // ...
    }
}
```

**修复后** (拷贝完成后才归还 buffer)：

```cpp
// grabFrame — 只 DQBUF，不归还，返回 bufIndex
bool V4l2Capture::grabFrame(void **data, int *size, int *bufIndex) {
    // ... select 等待 ...
    if (xioctl(m_fd, VIDIOC_DQBUF, &buf) < 0) return false;
    *data = m_buffers[buf.index].start;
    *size = buf.bytesused;
    *bufIndex = buf.index;  // 记录索引，用完再归还
    return true;
}

// returnBuffer — 新增方法，单独归还
bool V4l2Capture::returnBuffer(int bufIndex) {
    struct v4l2_buffer buf;
    memset(&buf, 0, sizeof(buf));
    buf.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    buf.memory = V4L2_MEMORY_MMAP;
    buf.index = bufIndex;
    return xioctl(m_fd, VIDIOC_QBUF, &buf) >= 0;
}

// run — cvtColor 完成后才归还
void V4l2Capture::run() {
    while (m_running) {
        void *data; int size; int bufIndex;
        grabFrame(&data, &size, &bufIndex);
        cv::Mat yuyv(m_height, m_width, CV_8UC2, data);
        cv::cvtColor(yuyv, bgr, cv::COLOR_YUV2BGR_YUYV); // 数据已拷贝到 bgr
        returnBuffer(bufIndex);                            // 现在才归还
        // ...
    }
}
```

**文件**：`src/capture/v4l2capture.h`、`src/capture/v4l2capture.cpp`

**状态**：已修复，亮线条花屏已消除，剩余轻微拖影属于刷新率/延迟的正常现象

---

## 问题 #8：开发板屏幕上右侧参数面板被挤压

**来源**：开发板实际运行测试

**现象**

右侧的参数调节控件被挤压，显示不完整。

**原因**

程序窗口固定为 960x560，但开发板屏幕分辨率只有 800x480，窗口超出屏幕范围，右侧控件被截断。

**修复**

用条件编译区分 ARM 和 x86，适配不同屏幕尺寸：

```cpp
// 窗口大小
#ifdef __arm__
    setFixedSize(800, 480);       // 开发板屏幕
#else
    setFixedSize(960, 660);       // 虚拟机
#endif

// 视频区域
#ifdef __arm__
    videoArea->setMinimumSize(480, 360);
    m_videoRect = QRect(4, 4, 480, 360);
#else
    videoArea->setMinimumSize(640, 480);
    m_videoRect = QRect(4, 4, 640, 480);
#endif
```

`__arm__` 是 ARM 编译器预定义的宏，交叉编译时自动定义，虚拟机本地编译时不定义。

**文件**：`src/ui/mainwindow.cpp`

**状态**：已修复，待验证

---

## 问题 #9：触摸事件穿透内窥镜界面到桌面程序

**日期**：2025-07

**现象**：运行内窥镜程序后，触摸操作会同时被内窥镜和开发板自带的 `/usr/local/qt-app/App` 程序接收，点击穿透到桌面图标。

**原因**：`linuxfb` 平台没有窗口管理器，所有 GUI 程序共享同一个 framebuffer 和输入设备，触摸事件会被所有监听程序同时接收。这是 linuxfb 的正常特性，不是 bug。

**解决方案**：运行内窥镜前先杀掉 App 进程：

```bash
kill $(pidof App) 2>/dev/null
sleep 0.5
/tmp/endoscope -platform linuxfb
```

**状态**：已解决

---

## 问题 #10：移动摄像头时画面有轻微拖影

**日期**：2025-07

**来源**：问题 #7 花屏修复后残留现象

**现象**

问题 #7 的亮线条花屏已消除，但快速移动摄像头时仍有轻微拖影（运动物体边缘有残影/模糊）。

**可能原因**

| 猜想 | 现象特征 | 排查方法 |
|---|---|---|
| 摄像头曝光时间过长 | 冻结帧也模糊，运动模糊 | 快速晃动后按冻结，看冻结帧是否清晰 |
| 图像处理耗时导致显示延迟 | 关闭处理后拖影减轻 | 关闭所有图像处理（CLAHE/去雾/降噪），只显示原始画面 |
| linuxfb 刷新机制 | 帧率正常但仍有拖影 | 观察 FPS 显示，确认帧率是否稳定在 24-30fps |

**排查步骤**

1. 观察 FPS 显示，确认帧率是否足够（≥24fps）
2. 快速晃动摄像头后按冻结，检查冻结帧是否清晰
   - 清晰 → 单帧没问题，是显示刷新导致
   - 模糊 → 摄像头曝光时间过长
3. 关闭所有图像处理，只显示原始画面
   - 拖影消失/减轻 → 处理管线耗时过长
   - 拖影不变 → linuxfb 本身的刷新机制

**状态**：待排查

---

## 问题 #11：开启白平衡/CLAHE后程序被OOM Killer杀掉

**日期**：2025-07

**现象**

在开发板上开启白平衡和CLAHE后，程序卡住，内核日志显示：

```
endoscope invoked oom-killer
total-vm:974928kB, anon-rss:864212kB
Out of memory: Kill process 2542 (endoscope) score 907
```

程序占用 864MB 内存（开发板总共约 960MB RAM），被系统强制杀掉。

**原因分析**

信号队列堆积导致内存耗尽。根本原因：

1. 采集线程以 30fps 持续 `emit frameReady(image.copy())`，每个信号携带一个 QImage 拷贝（~900KB）
2. `onFrameReady` 通过 `Qt::QueuedConnection` 跨线程投递到主线程事件队列
3. 开启图像处理后，`processFrame` 耗时增加（CLAHE + 白平衡 + 颜色空间转换），主线程处理速度跟不上采集速度
4. 信号在队列中持续堆积，每个信号都持有一个 QImage 拷贝，内存持续增长
5. 开发板无 swap 分区，内存耗尽后 OOM Killer 直接杀掉进程

```
采集线程 (30fps)          主线程事件队列              主线程处理
emit frame1 ──────→  [frame1]                    处理 frame1 (慢)
emit frame2 ──────→  [frame1, frame2]            还在处理 frame1...
emit frame3 ──────→  [frame1, frame2, frame3]    还在处理 frame1...
...                   队列越来越长，内存持续增长     ...
emit frameN ──────→  [frame1...frameN]           OOM!
```

**解决方案 — 版本 1：主线程丢帧标志**

思路：在主线程 `onFrameReady` 中加 `m_processing` 标志，处理中时丢弃新帧。

```cpp
// mainwindow.h
bool m_processing = false;

// mainwindow.cpp — onFrameReady
if (m_processing) return;  // 上一帧还没处理完，丢弃新帧
m_processing = true;
// ... 处理帧 ...
m_processing = false;
```

结果：**无效，仍然 OOM**。原因：信号通过 `Qt::QueuedConnection` 入队时，QImage 拷贝已经分配内存，`m_processing` 检查是在信号出队后才执行的，此时内存已经占用。

**解决方案 — 版本 2：采集线程端丢帧**

思路：将控制移到采集线程端，`emit` 之前检查标志，信号根本不发出，内存不会增长。

```cpp
// videosource.h — 新增 public 成员
std::atomic<bool> m_frameProcessing{false};

// v4l2capture.cpp — run() 中 emit 之前检查
if (m_frameProcessing.load()) continue;  // 主线程还没处理完，不发信号
m_frameProcessing.store(true);
emit frameReady(image.copy());

// mainwindow.cpp — onFrameReady 末尾清除标志
m_source->m_frameProcessing.store(false);
```

结果：**OOM 已解决，但 FPS 从 24 降到 13~15**。原因：丢帧时采集线程 `continue` 回到循环顶部，仍然执行 `grabFrame`（select 等待 ~33ms）+ `cvtColor`（YUYV→BGR），然后才检查标志并丢弃，每次丢帧浪费约 40~50ms。将检查移到 `grabFrame` 之前后，FPS 仅恢复到 14~15，仍不理想。撤回版本 2 后 FPS 恢复到 24~28，确认是此方案导致性能下降。

**解决方案 — 版本 3：共享缓存 + 无参信号（当前版本）**

思路：不阻止采集，不通过信号传递 QImage。采集线程全速运行，只更新共享缓存（永远只保存 1 帧），发无参信号通知主线程。

```cpp
// videosource.h — 新增共享缓存
QImage m_latestFrame;                    // 最新帧（采集线程写，主线程读）
QMutex m_frameMutex;                     // 保护共享缓存
std::atomic<bool> m_frameReady{false};   // 新帧就绪标志

// 信号改为无参
void frameReady();  // 不再携带 QImage

// v4l2capture.cpp — 采集线程写入共享缓存
{
    QMutexLocker lock(&m_frameMutex);
    m_latestFrame = image.copy();
}
if (!m_frameReady.exchange(true))  // 只在上一帧被取走后才发信号
    emit frameReady();

// mainwindow.cpp — 主线程从共享缓存取帧
void MainWindow::onFrameReady() {
    if (!m_source->m_frameReady.exchange(false)) return;  // 多余信号直接跳过
    QImage image;
    {
        QMutexLocker lock(&m_source->m_frameMutex);
        image.swap(m_source->m_latestFrame);
    }
    // ... 处理 image ...
}
```

与版本 2 的对比：

| | 版本 2（采集端丢帧） | 版本 3（共享缓存） |
|---|---|---|
| 信号携带 | QImage 拷贝（~900KB） | 无参数（0字节） |
| 队列堆积 | 阻止采集来避免 | 最多 1 个空信号，不占内存 |
| 采集线程 | 被阻塞，影响 FPS | 全速运行，不受影响 |
| 帧数据内存 | 队列中多份 | 共享缓存永远只有 1 帧 |

**文件**：`src/capture/videosource.h`、`src/capture/v4l2capture.cpp`、`src/capture/filecapture.cpp`、`src/ui/mainwindow.h`、`src/ui/mainwindow.cpp`

**状态**：版本 3 结果：OOM 已解决，但 FPS 从 24~28 降到 15~20（QMutex + image.copy 开销），开图像处理后 UI 卡死（空信号堆积）。版本 3b（加 exchange 控制）后 UI 不再卡死，但 FPS 仍低。

**解决方案 — 版本 4：atomic 计数器限制队列深度（当前版本）**

思路：回到最简单的直接 emit QImage 方式，去掉 QMutex 和共享缓存，只用一个 atomic 计数器限制队列中最多 2 帧。

```cpp
// videosource.h — 新增 public 成员
std::atomic<int> m_pendingFrames{0};

// v4l2capture.cpp — run() 中，归还 buffer 后检查队列深度
returnBuffer(bufIndex);
if (m_pendingFrames.load() >= 2) continue;  // 队列满，跳过 BGR→RGB 和 image.copy
cv::cvtColor(bgr, rgb, cv::COLOR_BGR2RGB);
m_pendingFrames.fetch_add(1);
emit frameReady(image.copy());

// mainwindow.cpp — onFrameReady 末尾
m_source->m_pendingFrames.fetch_sub(1);
```

与前三个版本的对比：

| | 版本 1 | 版本 2 | 版本 3 | 版本 4 |
|---|---|---|---|---|
| 方案 | 主线程丢帧标志 | 采集端丢帧标志 | 共享缓存+无参信号 | atomic 计数器 |
| OOM | 未解决 | 已解决 | 已解决 | 已解决 |
| 基础 FPS | - | 13~15 | 15~20 | ~22 |
| 额外开销 | 无 | 阻塞采集线程 | QMutex+共享拷贝 | 1个 atomic int |

**文件**：`src/capture/videosource.h`、`src/capture/v4l2capture.cpp`、`src/capture/filecapture.cpp`、`src/ui/mainwindow.h`、`src/ui/mainwindow.cpp`

**版本 4 结果**：基础 FPS ~22（接近原始 24~28），开白平衡后 FPS 降到 ~8，未 OOM，未卡死。开处理后 FPS 下降属于算法耗时问题，需单独优化。

> → FPS 问题的完整跟踪见 `16_FPS性能问题总跟踪.md`

**状态**：已修复

---

## 问题 #12：开启图像处理算法后 FPS 大幅下降

**日期**：2025-07

**现象**

- 基础 FPS（不开处理）：~22
- 开白平衡：FPS 降到 ~8
- 开多个算法：FPS 预计更低

**原因分析**

图像处理算法在主线程执行，每帧处理耗时过长。640x480 图像在 Cortex-A7 上各算法耗时待测量。

**排查结果**

各算法在 Cortex-A7 上的实测耗时（640x480，单帧）：

| 环节 | 平均耗时 |
|---|---|
| QImage→Mat | ~5ms |
| Mat→QImage | ~11ms |
| 白平衡 | ~40ms |
| CLAHE | ~115ms（首次 3380ms） |
| 去雾 | ~390ms |
| 锐化 | ~210ms |
| 降噪 | ~215ms |
| 边缘检测 | ~90ms |
| 阈值分割 | ~4ms |

每帧固定开销：QImage↔Mat 转换 ~16ms + src.clone() + scaled() + image.copy() 等。

**瓶颈分析**

1. 去雾最慢（390ms）：多次 split/merge/erode/divide，计算量大
2. 锐化（210ms）：GaussianBlur sigma=3.0 在 640x480 上开销大
3. 降噪（215ms）：双边滤波即使半分辨率仍然慢
4. CLAHE（115ms）：BGR→Lab→均衡→Lab→BGR 两次颜色空间转换
5. 白平衡（40ms）：split + mean + 乘法 + merge

**状态**：已优化

### 优化结果

| 算法 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| 白平衡 | ~40ms | ~9ms | **4.4x** |
| CLAHE | ~115ms（首次 3380ms） | ~28ms（首次 3308ms） | **4.1x** |
| 去雾 | ~390ms | ~87ms | **4.5x** |
| 锐化 | ~210ms | ~7ms | **30x** |
| 降噪 | ~215ms | ~55ms | **3.9x** |
| 边缘检测 | ~90ms | ~19ms | **4.7x** |
| 阈值分割 | ~4ms | ~1ms | **4x** |

降分辨率处理平均提升 4~5 倍，锐化因同时改用 3x3 固定核提升 30 倍。

CLAHE 首次调用仍然耗时 3308ms（OpenCV 内部初始化），复用对象后后续帧稳定在 ~28ms。

---

### 优化方案

#### 优化 1：降分辨率处理

将 640x480 缩小到 320x240 后再执行算法，处理完放大回原始尺寸。像素量减少 75%，耗时约降为 1/3~1/4。白平衡、CLAHE、去雾这类全局/区域算法在低分辨率下效果差别不大。

修改文件：`src/processing/imageprocessor.cpp` — `process()` 函数

修改前：
```cpp
void ImageProcessor::process(const cv::Mat &src, cv::Mat &dst,
                             const Config &cfg, ...) {
    dst = src.clone();
    // 直接在 640x480 上处理
    if (cfg.whiteBalance) applyWhiteBalance(dst, dst);
    if (cfg.clahe) applyCLAHE(dst, dst, ...);
    // ...
}
```

修改后：
```cpp
void ImageProcessor::process(const cv::Mat &src, cv::Mat &dst,
                             const Config &cfg, ...) {
    // 缩小到半分辨率处理
    cv::Mat work;
    cv::resize(src, work, cv::Size(), 0.5, 0.5, cv::INTER_LINEAR);
    // 在 320x240 上处理
    if (cfg.whiteBalance) applyWhiteBalance(work, work);
    if (cfg.clahe) applyCLAHE(work, work, ...);
    // ...
    // 放大回原始尺寸
    cv::resize(work, dst, src.size(), 0, 0, cv::INTER_LINEAR);
}
```

#### 优化 2：CLAHE 复用对象

当前每帧都 `cv::createCLAHE()` 创建新对象，首次调用耗时 3380ms。改为静态变量复用。

修改文件：`src/processing/imageprocessor.cpp` — `applyCLAHE()` 函数

修改前：
```cpp
void ImageProcessor::applyCLAHE(const cv::Mat &src, cv::Mat &dst,
                                double clipLimit, cv::Size tileSize) {
    cv::Mat lab;
    cv::cvtColor(src, lab, cv::COLOR_BGR2Lab);
    std::vector<cv::Mat> channels;
    cv::split(lab, channels);
    cv::Ptr<cv::CLAHE> clahe = cv::createCLAHE(clipLimit, tileSize);  // 每帧新建
    clahe->apply(channels[0], channels[0]);
    cv::merge(channels, lab);
    cv::cvtColor(lab, dst, cv::COLOR_Lab2BGR);
}
```

修改后：
```cpp
void ImageProcessor::applyCLAHE(const cv::Mat &src, cv::Mat &dst,
                                double clipLimit, cv::Size tileSize) {
    cv::Mat lab;
    cv::cvtColor(src, lab, cv::COLOR_BGR2Lab);
    std::vector<cv::Mat> channels;
    cv::split(lab, channels);
    static cv::Ptr<cv::CLAHE> clahe = cv::createCLAHE();  // 只创建一次
    clahe->setClipLimit(clipLimit);  // 动态更新参数
    clahe->setTilesGridSize(tileSize);
    clahe->apply(channels[0], channels[0]);
    cv::merge(channels, lab);
    cv::cvtColor(lab, dst, cv::COLOR_Lab2BGR);
}
```

#### 优化 3：锐化改用固定小核

当前 `GaussianBlur(src, blurred, Size(0,0), sigma=3.0)` 会自动计算大核（~19x19），在 Cortex-A7 上耗时 210ms。改用固定 3x3 核。

修改文件：`src/processing/imageprocessor.cpp` — `applySharpen()` 函数

修改前：
```cpp
void ImageProcessor::applySharpen(const cv::Mat &src, cv::Mat &dst,
                                  double sigma, double amount) {
    cv::Mat blurred;
    cv::GaussianBlur(src, blurred, cv::Size(0, 0), sigma);  // sigma=3.0, 核约 19x19
    cv::addWeighted(src, 1.0 + amount, blurred, -amount, 0, dst);
}
```

修改后：
```cpp
void ImageProcessor::applySharpen(const cv::Mat &src, cv::Mat &dst,
                                  double sigma, double amount) {
    cv::Mat blurred;
    cv::GaussianBlur(src, blurred, cv::Size(3, 3), 0);  // 固定 3x3 核
    cv::addWeighted(src, 1.0 + amount, blurred, -amount, 0, dst);
}
```

#### 优化 4：降噪进一步缩小

当前已经缩小到 0.5x 做双边滤波，但配合优化 1 的降分辨率处理，输入已经是 320x240，再缩 0.5x 变成 160x120，双边滤波更快。

此项无需额外修改代码，优化 1 自动生效。

---

### 优化结果

| 算法 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| 白平衡 | ~40ms | ~9ms | **4.4x** |
| CLAHE | ~115ms（首次 3380ms） | ~28ms（首次 3308ms） | **4.1x** |
| 去雾 | ~390ms | ~87ms | **4.5x** |
| 锐化 | ~210ms | ~7ms | **30x** |
| 降噪 | ~215ms | ~55ms | **3.9x** |
| 边缘检测 | ~90ms | ~19ms | **4.7x** |
| 阈值分割 | ~4ms | ~1ms | **4x** |

降分辨率处理平均提升 4~5 倍，锐化因同时改用 3x3 固定核提升 30 倍。

CLAHE 首次调用仍然耗时 3308ms（OpenCV 内部初始化），复用对象后后续帧稳定在 ~28ms。

### 进一步分析：算法耗时已优化但 FPS 仍低

白平衡算法优化后仅 9ms，加上 QImage↔Mat 转换 ~16ms，理论每帧 ~25ms，应能达到 ~40fps。但实际测试只有 8~9fps，说明瓶颈不在算法和格式转换，而在 `onFrameReady` 管线的其他环节。

在 `onFrameReady` 中对各环节加计时打印：

```cpp
void MainWindow::onFrameReady(const QImage &image) {
    QElapsedTimer t;
    t.start();
    QImage processed = processFrame(image);
    qDebug() << "[pipe] processFrame:" << t.elapsed() << "ms";

    t.start();
    m_frameBuffer.push_back(processed.copy());  // 8帧深拷贝缓存
    if (m_frameBuffer.size() > FRAME_BUFFER_SIZE) m_frameBuffer.pop_front();
    qDebug() << "[pipe] frameBuffer:" << t.elapsed() << "ms";

    t.start();
    m_displayImage = processed.scaled(m_videoRect.size(), ...);
    qDebug() << "[pipe] scaled:" << t.elapsed() << "ms";
    // ...
}
```

**待测试**：开白平衡后观察 `[pipe]` 输出，定位管线中哪个环节是真正的瓶颈。

### 管线耗时测量结果

不开处理时（processFrame: 0ms）：

| 环节 | 耗时 |
|---|---|
| processFrame | 0ms |
| frameBuffer copy | ~5ms |
| scaled | ~16ms |
| **合计** | **~21ms** |

开白平衡时（processFrame: ~67ms）：

| 环节 | 耗时 |
|---|---|
| processFrame | ~67ms = QImage→Mat(5ms) + resize缩小 + 白平衡(9ms) + resize放大 + Mat→QImage(11ms) |
| frameBuffer copy | ~5ms |
| scaled | ~16ms |
| **合计** | **~88ms → ~11fps** |

**关键发现**：

1. `scaled` 每帧固定 16ms，是最大的固定开销
2. `frameBuffer copy` 每帧 5ms，8 帧深拷贝浪费
3. `process()` 末尾 resize 放大回 640x480，然后 `scaled` 又缩小到显示尺寸，重复缩放

### 优化 5：管线开销优化

修改文件：`src/ui/mainwindow.cpp`、`src/processing/imageprocessor.cpp`

#### 5a. 去掉 frameBuffer 深拷贝（省 ~5ms/帧）

QImage 使用隐式共享（copy-on-write），赋值只增加引用计数，不拷贝数据。

修改前：
```cpp
m_frameBuffer.push_back(processed.copy());  // 每帧深拷贝 ~5ms
```

修改后：
```cpp
m_frameBuffer.push_back(processed);  // 隐式共享，~0ms
```

#### 5b. 去掉 QImage::scaled（省 ~16ms/帧）

之前 `process()` 末尾 resize 放大回 640x480，然后 `onFrameReady` 中又 `scaled` 缩小到显示尺寸，重复缩放。现在 `process()` 直接输出半分辨率，在 `processFrame` 中用 OpenCV resize 一次性缩放到显示尺寸。

`src/processing/imageprocessor.cpp` 修改前：
```cpp
void ImageProcessor::process(...) {
    cv::Mat work;
    cv::resize(src, work, cv::Size(), 0.5, 0.5, cv::INTER_LINEAR);
    // ... 算法处理 ...
    cv::resize(work, dst, src.size(), 0, 0, cv::INTER_LINEAR);  // 放大回 640x480
}
```

`src/processing/imageprocessor.cpp` 修改后：
```cpp
void ImageProcessor::process(...) {
    cv::Mat work;
    cv::resize(src, work, cv::Size(), 0.5, 0.5, cv::INTER_LINEAR);
    // ... 算法处理 ...
    dst = work;  // 直接输出半分辨率
}
```

`src/ui/mainwindow.cpp` — `processFrame()` 修改前：
```cpp
if (!anyEnabled) return input;  // 返回 640x480 原图

cv::Mat src = ImageConvert::qimageToMat(input);
cv::Mat dst;
ImageProcessor::process(src, dst, ...);  // 输出 640x480
return ImageConvert::matToQImage(dst);
// 然后在 onFrameReady 中再 scaled 到显示尺寸 (~16ms)
```

`src/ui/mainwindow.cpp` — `processFrame()` 修改后：
```cpp
if (!anyEnabled) {
    // 不开处理时也缩放到显示尺寸
    return input.scaled(m_videoRect.size(), Qt::KeepAspectRatio, Qt::FastTransformation);
}

cv::Mat src = ImageConvert::qimageToMat(input);
cv::Mat dst;
ImageProcessor::process(src, dst, ...);  // 输出 320x240
// 直接 resize 到显示尺寸，省掉放大回 640x480 再 scaled 的重复操作
cv::Mat display;
cv::resize(dst, display, cv::Size(m_videoRect.width(), m_videoRect.height()));
return ImageConvert::matToQImage(display);
```

`src/ui/mainwindow.cpp` — `onFrameReady()` 修改前：
```cpp
m_frameBuffer.push_back(processed.copy());  // 深拷贝 ~5ms
// ...
m_displayImage = processed.scaled(m_videoRect.size(), ...);  // ~16ms
```

`src/ui/mainwindow.cpp` — `onFrameReady()` 修改后：
```cpp
m_frameBuffer.push_back(processed);  // 隐式共享 ~0ms
// ...
m_displayImage = processed;  // 已在 processFrame 中缩放好，直接赋值
```

同时去掉了 `imageprocessor.cpp` 中的计时代码（已完成测量）。

预期每帧节省：~21ms（frameBuffer 5ms + scaled 16ms）+ resize 放大的开销。

**优化 5 结果**：

| 场景 | 优化前 | 优化后 | 提升 |
|---|---|---|---|
| 不开处理 | ~22fps | 20~23fps | 持平 |
| 白平衡 | 8~9fps | **16~18fps** | **2x** |
| CLAHE | 估计~5fps | **11~13fps**（首次 3fps） | **~2.5x** |

#### 优化 6：CLAHE 启动预热

CLAHE 首次调用耗时 ~3s（OpenCV 内部初始化），导致开启 CLAHE 后第一帧卡顿。在程序启动时用小图预热一次。

修改文件：`src/ui/mainwindow.cpp` — 构造函数末尾

```cpp
// 构造函数末尾新增：
{
    cv::Mat dummy = cv::Mat::zeros(8, 8, CV_8UC3);
    cv::Mat out;
    ImageProcessor::applyCLAHE(dummy, out);  // 触发 static CLAHE 对象创建
}
```

效果：启动时多等 ~3s，但运行时开启 CLAHE 不再卡顿。

**状态**：已验证，CLAHE 首次开启不再卡顿

### 问题 #12 最终结果

| 场景 | 原始 | 优化后 | 提升 |
|---|---|---|---|
| 不开处理 | ~22fps | 20~23fps | 持平 |
| 白平衡 | ~8fps | **16~18fps** | **2x** |
| CLAHE | ~5fps（首次卡 3s） | **11~13fps**（首次不卡） | **~2.5x** |
| 去雾 | ~2fps | 预计 ~8fps | **~4x** |
| 锐化 | ~4fps | 预计 ~20fps | **~5x** |
| 降噪 | ~4fps | 预计 ~12fps | **~3x** |

**状态**：已优化

> → FPS 问题的完整跟踪见 `16_FPS性能问题总跟踪.md`

### 降分辨率处理的画质风险

优化 1 将处理分辨率从 640x480 降到 320x240，处理完后放大到显示尺寸（480x360），放大倍率 1.5x。对不同算法的画质影响：

| 算法 | 影响程度 | 原因 |
|---|---|---|
| 白平衡 | 小 | 全局通道均值计算，不依赖分辨率 |
| CLAHE | 小 | 按区块做直方图均衡，320x240 区块数足够 |
| 阈值分割 | 小 | 像素级操作，放大后边缘稍模糊但可接受 |
| 降噪 | 小 | 本身就是模糊操作，低分辨率影响不大 |
| 锐化 | 中 | 低分辨率上锐化再放大，细节增强效果打折 |
| 去雾 | 中 | 暗通道先验依赖局部最小值，低分辨率会丢失细节 |
| 边缘检测 | 中 | 低分辨率会丢失细边缘 |

当前显示屏 800x480，视频区域 480x360，320x240 放大 1.5 倍到 480x360，画质损失不明显。如果后续换更高分辨率屏幕或对画质有更高要求，可以针对特定算法（锐化、去雾）改回全分辨率处理。

---
## 问题 #13：V4L2 驱动实际分配的 buffer 数量少于请求数量

**日期**：2025-01

**现象**

在开发板上运行时，内核日志中持续出现 DCMI overrun 错误：

```bash
dmesg | grep dcmi
stm32-dcmi 4c006000.dcmi: Some errors found while streaming: errors=3721 (overrun=3724), buffers=46631
```

同时通过 `v4l2-ctl` 查看实际运行参数时发现：

```bash
v4l2-ctl -d /dev/video0 --all | grep "Read buffers"
Read buffers     : 2
```

但代码中明确请求的是 4 个 buffer。

**问题发现过程**

### 1. 观察到 overrun 错误

在用户提供的 `v4l2-ctl --all` 输出中，注意到 `Read buffers: 2`，这引起了怀疑。

### 2. 检查代码中的 buffer 请求

**步骤 1：查看构造函数默认参数**

```bash
grep -n "V4l2Capture(" src/capture/v4l2capture.h
```

输出：
```cpp
V4l2Capture(const std::string &device = "/dev/video0",
            int width = 640, int height = 480,
            int fps = 30, int bufferCount = 4,  // ← 默认 4 个
            QObject *parent = nullptr);
```

**步骤 2：查看配置文件**

```bash
grep "buffers" config/endoscope.conf
```

输出：
```ini
buffers=4
```

**步骤 3：查看代码中如何使用配置**

```bash
grep -B10 "new V4l2Capture" src/ui/mainwindow.cpp | grep "buffers"
```

输出：
```cpp
int buffers = settings.value("camera/buffers", 4).toInt();
auto *v4l2 = new V4l2Capture(device.toStdString(), width, height,
                             fps, buffers, this);
```

**步骤 4：查看 V4L2 请求 buffer 的代码**

```bash
grep -n "requestbuffers\|req.count" src/capture/v4l2capture.cpp
```

输出：
```cpp
213:    struct v4l2_requestbuffers req;
215:    req.count = m_bufferCount;                // 请求的缓冲区数量（通常 4 个）
226:    m_buffers.resize(req.count);
227:    for (unsigned i = 0; i < req.count; i++) {
```

**结论**：代码确实请求了 4 个 buffer。

### 3. 理解 V4L2 的 buffer 分配机制

V4L2 的 `VIDIOC_REQBUFS` ioctl 工作机制：

```c
// 应用程序请求
struct v4l2_requestbuffers req;
req.count = 4;  // 请求 4 个
ioctl(fd, VIDIOC_REQBUFS, &req);

// 驱动返回（可能修改 req.count）
// req.count = 2;  // 实际只能分配 2 个
```

**关键点**：驱动可以分配**少于**请求数量的 buffer，并通过修改 `req.count` 告知应用程序。

### 4. 验证驱动实际分配的数量

**方法 1：通过 v4l2-ctl 查看（已验证）**

```bash
v4l2-ctl -d /dev/video0 --all | grep "Read buffers"
```

输出：`Read buffers     : 2`

**方法 2：在代码中添加日志验证（建议添加）**

在 `src/capture/v4l2capture.cpp` 的 `requestBuffers()` 函数中添加：

```cpp
bool V4l2Capture::requestBuffers() {
    struct v4l2_requestbuffers req;
    memset(&req, 0, sizeof(req));
    req.count = m_bufferCount;  // 请求 4 个
    req.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    req.memory = V4L2_MEMORY_MMAP;

    if (xioctl(m_fd, VIDIOC_REQBUFS, &req) < 0) {
        qCritical() << "VIDIOC_REQBUFS failed";
        return false;
    }

    // 添加日志：检查实际分配的数量
    qWarning() << "Requested" << m_bufferCount << "buffers, driver allocated" << req.count;
    
    if (req.count < 2) {
        qCritical() << "Insufficient buffer memory";
        return false;
    }

    m_buffers.resize(req.count);  // 使用实际分配的数量
    // ...
}
```

**原因分析**

驱动只分配 2 个 buffer 的可能原因：

1. **驱动限制**：`stm32-dcmi` 驱动可能硬编码了最大 buffer 数量为 2
2. **内存限制**：DCMI 驱动预留的 DMA 连续内存不足以分配 4 个 buffer
3. **内核配置**：设备树或内核配置中限制了 buffer 数量

### 5. overrun 错误的根本原因

**overrun 发生机制**：

```
时间线（只有 2 个 buffer）：
T0: DQBUF buffer0 → CPU 开始处理（cvtColor + 图像处理）
T1: 摄像头写入 buffer1
T2: 摄像头写入完 buffer1，但 buffer0 还在 CPU 处理中
T3: 摄像头没有空闲 buffer，只能覆盖 buffer0 → overrun!
    （驱动报错：errors=3721 (overrun=3724)）
```

**为什么 2 个 buffer 不够？**

- 摄像头帧间隔：33ms（30fps）
- CPU 处理时间：
  - 不开处理：~20ms（勉强够用）
  - 开白平衡：~45ms（超时，发生 overrun）
  - 开多个算法：~100ms+（严重 overrun）

**如果有 4 个 buffer：**

```
T0: DQBUF buffer0 → CPU 处理
T1: 摄像头写入 buffer1
T2: 摄像头写入 buffer2
T3: 摄像头写入 buffer3
T4: buffer0 处理完，归还 → 摄像头有空闲 buffer，不会 overrun
```

**解决方案**

### 方案 1：优化处理速度（推荐，继续执行）

**验证结果表明**：有 4 个 buffer，但处理速度仍然是瓶颈。

**目标**：将处理时间降到 < 100ms（理想 < 50ms），确保不超过 buffer 缓冲能力。

1. ✅ 启用 OV5640 硬件白平衡（省 9ms）
2. ✅ 降分辨率处理（已做，提升 4~5x）
3. ✅ 管线优化（去掉重复拷贝和缩放，省 ~21ms）
4. 🔲 NEON 指令集加速（P5-P6，预计提升 2-5x）
5. 🔲 多线程流水线（P3，采集、处理、显示分离）

**目标**：将处理时间降到 30ms 以内，使 2 个 buffer 够用。

### 方案 2：尝试增加驱动的 buffer 数量（高级，有风险）

如果有内核源码和编译环境，可以尝试修改驱动：

**步骤 1：查找驱动源码中的限制**

```bash
# 在内核源码中查找
grep -r "MAX_BUFFER" drivers/media/platform/stm32/
```

可能的限制位置：
```c
// drivers/media/platform/stm32/stm32-dcmi.c
#define MAX_BUFFER_NUM 2  // ← 可能的限制
```

**步骤 2：修改并重新编译内核**

```c
#define MAX_BUFFER_NUM 4  // 从 2 改为 4
```

**步骤 3：检查 DMA 内存是否足够**

在设备树中检查 reserved-memory：

```bash
cat /sys/firmware/devicetree/base/reserved-memory/*/size
```

**风险**：
- 需要重新编译内核
- 可能导致系统不稳定
- 有变砖风险

### 方案 3：验证是否真的只有 2 个（待验证）

在代码中添加日志后运行，确认驱动实际分配的数量：

```bash
# 在开发板上运行程序，查看日志
./endoscope -platform linuxfb 2>&1 | grep "buffers"
```

预期输出：
```
Requested 4 buffers, driver allocated 2
```

**验证结果**（2025-01）

在开发板上运行程序，实际日志输出：

```
Driver: stm32-dcmi Card: STM32 Camera Memory Interface Bus: platform:dcmi
支持的像素格式:
  [0] JFIF JPEG (0x4745504a)
  [1] UYVY 4:2:2 (0x59565955)
  [2] YUYV 4:2:2 (0x56595559)
  [3] 16-bit RGB 5-6-5 (0x50424752)
[Buffer 验证] 请求 4 个 buffer，驱动实际分配 4 个
V4L2 就绪: 640x480 @ 30fps, 4 buffers (实际分配)
```

**重要发现**：

✅ **驱动实际分配了 4 个 buffer**，而不是之前 `v4l2-ctl --all` 显示的 2 个！

**v4l2-ctl 输出不一致的原因**：

`v4l2-ctl --all` 显示的 `Read buffers: 2` 可能是：
1. ✅ **程序未运行时的默认值**（最可能）
   - 程序运行时设备被独占，`v4l2-ctl` 无法访问
   - 验证：程序运行时执行 `v4l2-ctl` 无输出（设备被锁定）
2. 驱动的默认配置值（不是实际运行时的值）
3. 指的是驱动内部的读缓冲区，不是 mmap buffer 数量

**结论修正**：

❌ **之前的假设错误**：overrun 不是因为 buffer 数量不足（实际有 4 个）

✅ **真正的原因**：**处理速度过慢**

```
时间线分析（有 4 个 buffer）：
T0:   DQBUF buf0 → CPU 处理（耗时 100ms+）
T33:  摄像头写入 buf1
T66:  摄像头写入 buf2  
T99:  摄像头写入 buf3
T132: 摄像头需要第 5 个 buffer，但 buf0 还在处理中 → overrun!
```

**关键数据**：
- 帧间隔：33ms（30fps）
- 4 个 buffer 能缓冲：4 × 33ms = 132ms
- 处理时间：
  - 不开处理：~20ms ✅ 不会 overrun
  - 开白平衡：~45ms ✅ 不会 overrun
  - 开多个算法：~100ms+ ❌ 会 overrun

**如果处理时间 > 132ms，即使有 4 个 buffer 也会 overrun。**

**当前状态**

- ✅ 已确认代码请求 4 个 buffer
- ✅ 已添加日志验证代码（`src/capture/v4l2capture.cpp`）
- ✅ **已验证：驱动实际分配了 4 个buffer**
- ✅ **已验证：程序运行时设备被独占，`v4l2-ctl` 无法访问**
- ❌ 之前的假设错误：overrun 不是因为 buffer 数量不足
- ✅ **真正原因：处理速度过慢**（处理时间 > 132ms 时会 overrun）

**学习总结**

这次验证展示了重要的调试思维：

1. ✅ **不要盲目相信工具输出**
   - `v4l2-ctl` 在程序未运行时显示的是默认值
   - 程序运行时设备被独占，工具无法访问

2. ✅ **用代码验证假设**
   - 添加日志直接查看驱动返回的值
   - 这是最可靠的验证方法

3. ✅ **假设被推翻也是有价值的**
   - 虽然假设错了，但排除了一个可能性
   - 明确了真正的问题：处理速度

4. ✅ **理解设备独占机制**
   - V4L2 设备在 streaming 时被独占锁定
   - 其他进程无法同时访问

**建议**

1. **短期**：继续优化处理速度，使 2 个 buffer 够用
2. **中期**：在代码中添加日志验证，确认驱动行为
3. **长期**：如果性能仍不足，考虑研究驱动源码，评估修改风险

**相关文件**

- `src/capture/v4l2capture.h` - 构造函数默认参数
- `src/capture/v4l2capture.cpp` - `requestBuffers()` 函数
- `config/endoscope.conf` - buffer 配置
- `src/ui/mainwindow.cpp` - 读取配置并创建 V4l2Capture

**参考资料**

- V4L2 API 文档：https://www.kernel.org/doc/html/latest/userspace-api/media/v4l/vidioc-reqbufs.html
- STM32 DCMI 驱动源码：`drivers/media/platform/stm32/stm32-dcmi.c`

---

## 问题 #14：Overrun 的真正机制 — 线程调度问题

**日期**：2025-01

**关键测试数据**（开启 CLAHE 后）：

| 指标 | 数值 |
|---|---|
| 采集线程平均耗时 | 48.4 ms/帧 |
| 总帧数 | 4441 帧 |
| Overrun 次数 | 669 次 |
| Overrun 率 | 15.06% |
| 运行时长 | ~148 秒 |
| 平均 overrun 频率 | 4.5 次/秒 |

**对比分析**：

| 场景 | 采集线程耗时 | Overrun 次数 | Overrun 率 | 结论 |
|---|---|---|---|---|
| 不开处理 | 37.5 ms | 0 | 0% | ✅ 正常 |
| 开启 CLAHE | 48.4 ms | 669 | 15.06% | ❌ 严重 |

**关键发现**：

采集线程耗时增加了 10.9ms（从 37.5ms → 48.4ms），但采集线程只做 YUYV→BGR→RGB 转换，不应该受 CLAHE 影响！

**Overrun 的真正原因**：

❌ **之前的假设错误**：不是 buffer 数量不足（实际有 4 个），也不是简单的处理时间过长

✅ **真正的原因**：**系统整体负载过高导致的线程调度问题**

**机制分析**：

```
1. 主线程处理 CLAHE 很慢（~28ms）+ QImage↔Mat 转换（~16ms）= ~44ms
2. 主线程处理不过来，信号队列堆积
3. 采集线程被 m_pendingFrames >= 2 限制，频繁跳帧
4. 采集线程跳帧时仍执行 grabFrame（select 等待）+ cvtColor，占用 CPU
5. 系统整体负载高，采集线程的调度优先级受影响
6. 采集线程响应变慢（37.5ms → 48.4ms）
7. 采集线程来不及归还 buffer → 4 个 buffer 全部被占用 → overrun!
```

**关键数据**：

- 4 个 buffer 缓冲能力：4 × 33ms = 132ms
- 当前处理时间：48.4ms（采集）+ 44ms（主线程）= 92.4ms
- 理论上不应该 overrun，但实际 overrun 率 15%

**说明**：不是简单的处理时间问题，而是线程调度和队列堆积导致的系统性问题。几乎每 4-5 帧就有 1 次 overrun，问题非常严重。

**优化方向**：

**短期（立即可做）**：

1. ✅ 启用 OV5640 硬件白平衡（省掉软件白平衡的 9ms）
2. 增加 `m_pendingFrames` 限制（从 2 改为 4，允许更多帧在队列中，但可能增加延迟）

**中期（需要重构）**：

1. 多线程流水线：采集线程 → 处理线程 → 主线程（只显示），解耦采集和处理
2. NEON 加速：加速 YUYV→RGB 转换和 CLAHE 等算法

**精准定位方法**：

1. 测试不同 `m_pendingFrames` 限制值（2/4/8）对 overrun 率的影响
2. 测试关闭主线程处理（只采集不处理）时的 overrun 率
3. 添加采集线程的 CPU 占用率监控
4. 测试不同算法组合的 overrun 率（白平衡、CLAHE、去雾等）

**相关文件**：

- `src/capture/v4l2capture.cpp` - 采集线程实现
- `src/ui/mainwindow.cpp` - 主线程处理
- `docs/测试指南_Overrun手动测试.md` - 测试记录

**状态**：已定位根本原因，待优化

> → FPS 问题的完整跟踪见 `16_FPS性能问题总跟踪.md`

---

## 问题 #15：录像时 VideoWriter 尺寸不匹配导致崩溃

**日期**：2025-01

**现象**

点击录像后程序崩溃：

```
OpenCV(3.4.16) cap_mjpeg_encoder.cpp:476: error: (-215:Assertion failed)
imgWidth == frameWidth && imgHeight == frameHeight && channels == 3 in function 'write'
```

**原因**

`VideoWriter` 打开时使用的尺寸和实际写入帧的尺寸不匹配。经历了两次修复：

1. **第一次**：`VideoWriter` 用 640x480（采集尺寸）打开，但 `processed` 已经是显示尺寸（460x360），不匹配
2. **第二次**：改用 `m_videoRect` 尺寸打开后，旋转 90° 时图像变成竖向（360x460），又不匹配

**修复**

写入前统一 resize 到 `VideoWriter` 的尺寸，无论旋转与否都不会崩溃：

```cpp
// 修复前：直接写入，尺寸可能不匹配
m_videoWriter.write(mat);

// 修复后：写入前强制 resize
cv::Mat resized;
cv::resize(mat, resized, cv::Size(m_videoRect.width(), m_videoRect.height()));
m_videoWriter.write(resized);
```

**文件**：`src/ui/mainwindow.cpp`

**状态**：已修复

---

## 问题 #16：从图像编辑模式返回后视频采集无法恢复

**日期**：2025-01

**现象**

进入图像编辑模式后返回调试或临床模式，视频画面不恢复，终端持续输出"采集帧超时"。

**原因分析**

进入编辑模式时调用 `m_source->stop()` 设置 `m_running = false`，采集线程的 `run()` 循环检测到后退出，并在末尾调用 `stopStreaming()` 关闭视频流。

返回时调用 `m_source->start()` 尝试重启线程，但此时旧线程可能还没完全退出（`run()` 末尾的 `stopStreaming()` 还在执行），QThread 发现线程仍在运行，忽略了 `start()` 调用。等旧线程真正退出后，没有人再调用 `start()`，采集就永久停止了。

```
进入编辑模式：
  stop()          → m_running = false
  （线程还在执行 stopStreaming...）

返回调试模式：
  start()         → QThread 发现线程还在运行，忽略！
  （线程退出后无人再 start）
  → 采集永久停止，"采集帧超时"
```

**修复**

在 `stop()` 之后加 `wait()` 等待线程完全退出，在 `start()` 之前也加 `wait()` 确保线程已退出：

**改动前：**

```cpp
// 进入编辑模式
case 2:
    if (m_source && m_source->isRunning())
        m_source->stop();
    break;

// 离开编辑模式
if (m_currentMode == 2 && mode != 2) {
    if (m_source && !m_source->isRunning())
        m_source->start();
}
```

**改动后：**

```cpp
// 进入编辑模式：stop + wait 确保线程完全退出
case 2:
    if (m_source) {
        m_source->stop();
        m_source->wait();  // 等待线程完全退出
    }
    break;

// 离开编辑模式：wait + start 确保线程已退出再重启
if (m_currentMode == 2 && mode != 2) {
    if (m_source) {
        m_source->wait();  // 确保线程完全退出
        m_source->start(); // 重新启动采集线程
    }
}
```

**关键点**：QThread 的 `start()` 只能在线程未运行时调用，如果线程还在退出过程中（`run()` 末尾清理），`start()` 会被忽略。`wait()` 阻塞等待线程真正结束后再 `start()`，保证重启成功。

**文件**：`src/ui/mainwindow.cpp` — `switchToMode()` 函数

**状态**：已修复

---

### 问题 #16 补充：第一版修复无效，第二版方案

**第一版修复为什么无效**

第一版在进入编辑模式时 `stop()` + `wait()` 杀死采集线程，返回时 `wait()` + `start()` 重启线程。日志显示：

```
[MODE] wait 完成, 准备 start
[MODE] start 完成, isRunning: false    ← start() 调用了但线程没启动
采集帧超时
```

原因：V4L2 的 `run()` 方法在退出时调用了 `stopStreaming()`（即 `VIDIOC_STREAMOFF`），这会取消所有 buffer 的排队。再次 `start()` 时 `run()` 开头调用 `startStreaming()`（即 `VIDIOC_STREAMON`），但此时所有 buffer 都已被取消排队，没有可用的 buffer 给驱动填充数据，导致采集失败。

```
第一次启动：
  open() → requestBuffers() → QBUF 所有 buffer → run() → STREAMON ✅

stop + start 后：
  run() 退出 → STREAMOFF（所有 buffer 被取消排队）
  run() 重新开始 → STREAMON（但没有 buffer 在队列中）→ 采集失败 ❌
```

要修复需要在 `startStreaming()` 前重新 QBUF 所有 buffer，但这改动较大且容易引入新问题。

**第二版方案：pause/resume 代替 stop/start**

核心思路：**线程始终运行，不杀死**。用 `pause()`/`resume()` 控制是否处理帧。暂停时线程仍然 grabFrame + returnBuffer 保持 V4L2 流活跃，只是不 emit 帧数据。

**videosource.h — 新增 pause/resume，新增 m_quit 标志**

改动前：

```cpp
    void stop() { m_running = false; }
    bool isRunning() const { return m_running; }

    // ...
    std::atomic<bool> m_running;
```

改动后：

```cpp
    // 暂停采集：线程保持运行但不处理帧
    void pause() { m_running = false; }

    // 恢复采集
    void resume() { m_running = true; }

    // 停止线程：线程完全退出
    void stop() { m_quit = true; m_running = false; }

    bool isRunning() const { return m_running; }

    // ...
    std::atomic<bool> m_running;
    std::atomic<bool> m_quit{false};
```

**v4l2capture.cpp — run() 循环用 m_quit 控制退出，m_running 控制暂停**

改动前：

```cpp
void V4l2Capture::run() {
    if (!startStreaming()) return;
    m_running = true;
    // ...
    while (m_running) {
        if (!grabFrame(&data, &size, &bufIndex)) {
            if (m_running)
                qDebug() << "采集帧超时";
            continue;
        }

        perfTimer.start();
        // ... 处理帧 ...
    }
    stopStreaming();
}
```

改动后：

```cpp
void V4l2Capture::run() {
    if (!startStreaming()) return;
    m_running = true;
    // ...
    while (!m_quit) {
        if (!grabFrame(&data, &size, &bufIndex)) {
            if (!m_quit)
                qDebug() << "采集帧超时";
            continue;
        }

        // 暂停时只归还 buffer，不处理帧
        if (!m_running) {
            returnBuffer(bufIndex);
            continue;
        }

        perfTimer.start();
        // ... 处理帧 ...
    }
    stopStreaming();
}
```

**filecapture.cpp — run() 同样改为 m_quit 控制退出**

改动前：

```cpp
void FileCapture::run() {
    m_running = true;
    while (m_running) {
        // ... 读帧 + emit ...
    }
}
```

改动后：

```cpp
void FileCapture::run() {
    m_running = true;
    while (!m_quit) {
        if (!m_running) {
            QThread::msleep(50);
            continue;
        }
        // ... 读帧 + emit ...
    }
}
```

**mainwindow.cpp — switchToMode() 用 pause/resume 代替 stop/start**

改动前：

```cpp
// 离开编辑模式
if (m_currentMode == 2 && mode != 2) {
    if (m_source) {
        m_source->wait();
        m_source->start();
    }
}

// 进入编辑模式
case 2:
    if (m_source) {
        m_source->stop();
        m_source->wait();
    }
```

改动后：

```cpp
// 离开编辑模式
if (m_currentMode == 2 && mode != 2) {
    if (m_source)
        m_source->resume();
}

// 进入编辑模式
case 2:
    if (m_source)
        m_source->pause();
```

**两版方案对比**

| | 第一版（stop/start） | 第二版（pause/resume） |
|---|---|---|
| 线程状态 | 杀死再重启 | 始终运行 |
| V4L2 流 | STREAMOFF → STREAMON（失败） | 始终保持 STREAMON |
| 暂停时 CPU | 线程退出，零开销 | 线程仍在 grabFrame + returnBuffer |
| 恢复速度 | 慢（需重启线程） | 即时（设置标志位） |
| 可靠性 | ❌ V4L2 重启失败 | ✅ 稳定 |

**状态**：第二版已修复

---

## 问题 #17：临床模式切回调试模式后 FPS 不恢复

**日期**：2025-01

**现象**

进入临床模式（支气管镜模式）后 FPS 降到 ~4fps，切回调试模式后即使所有算法都没勾选，FPS 也只有 ~10fps，无法恢复到正常的 ~20fps。

**原因**

三线程管线中，处理线程的算法配置（`m_processThread->config`）通过 displayTimer 定时同步。临床模式下预设参数开启了锐化/降噪等算法，`config` 被设为预设值。

切回调试模式时，同步代码只更新了 clahe/undistort/dehaze，但 sharpen/denoise/edgeDetect/threshold 还保留着临床模式的值（true），处理线程继续跑这些算法。

```cpp
// 改动前：调试模式只同步了部分字段
if (!m_clinicalMode) {
    m_processThread->config.clahe = m_chkClahe->isChecked();
    m_processThread->config.dehaze = m_chkDehaze->isChecked();
    // sharpen/denoise/edgeDetect/threshold 没有重置！
    // 还保留着临床模式预设的 true 值
}
```

**修复**

调试模式下显式把通用算法字段设为 false：

```cpp
// 改动后：调试模式同步所有字段
if (!m_clinicalMode) {
    m_processThread->config.clahe = m_chkClahe->isChecked();
    m_processThread->config.dehaze = m_chkDehaze->isChecked();
    // 通用算法已移至图像编辑页，实时流中固定关闭
    m_processThread->config.sharpen = false;
    m_processThread->config.denoise = false;
    m_processThread->config.edgeDetect = false;
    m_processThread->config.threshold = false;
}
```

**文件**：`src/ui/mainwindow.cpp` — displayTimer 回调中的参数同步

**状态**：已修复

---
